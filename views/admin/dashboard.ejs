<div class="admin-title">
  <h2>Posts</h2>
  <a href="/add-post" class="button">+ Adicionar</a>
</div>

<ul class="admin-posts">

  <% posts.forEach(post => { %>
   
    <li>
      <a href="/post/<%= post._id %>">
        <%= post.title %> &nearr;
      </a>
      <div class="admin-post-controls">
        <a href="/edit-post/<%= post._id %>" class="btn">Editar</a>

        <form action="/delete-post/<%= post._id %>?_method=DELETE" method="POST">
          <input type="submit" value="Deletar" class="btn-delete btn">
        </form>

      </div>

    </li>

  <% }) %>
</ul>

<div class="admin-title">
  <h2>Categorias</h2>
  <a href="/add-category" class="button">+ Adicionar</a>
</div>

<ul class="admin-posts">

  <% categories.forEach(category => { %>
   
    <li>
      <a href="/post/<%= category._id %>">
        <%= category.name %> &nearr;
      </a>
      <div class="admin-post-controls">
        <a href="/edit-category/<%= category._id %>" class="btn">Editar</a>

        <form action="/delete-category/<%= category._id %>?_method=DELETE" method="POST">
          <input type="submit" value="Deletar" class="btn-delete btn">
        </form>

      </div>

    </li>

  <% }) %>
</ul>

<div class="admin-title">
  <h2>Users</h2>
  <a href="/add-user" class="button">+ Adicionar</a>
</div>

<ul class="admin-posts">

  <% users.forEach(user => { %>
   
    <li>
      <a href="/post/<%= user._id %>">
        <%= user.username %> &nearr;
      </a>
      <div class="admin-post-controls">
        <a href="/edit-user/<%= user._id %>" class="btn">Editar</a>

        <form action="/delete-user/<%= user._id %>?_method=DELETE" method="POST">
          <input type="submit" value="Deletar" class="btn-delete btn">
        </form>

      </div>

    </li>

  <% }) %>
</ul>